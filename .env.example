# Copy to `.env`. No API keys — use `az login` + Azure AI Projects SDK (see readme.txt).

# Foundry portal: your project endpoint (often …/api/projects/<name>)
AZURE_AI_PROJECT_ENDPOINT=

# Foundry → Build → Models → Deployments (deployment name column)
AZURE_AI_MODEL_DEPLOYMENT_NAME=DeepSeek-V3.2
